openapi: 3.0.0
info:
  title: Httpbin API via Kong
  version: 1.0.0

servers:
  - url: https://httpbin.org

paths:
  /anything:
    get:
      summary: Echo anything (GET)
      operationId: getAnything
      responses:
        "200":
          description: Successful response
